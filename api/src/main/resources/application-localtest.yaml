logging:
  level:
    root: INFO
    io.kafbat.ui: DEBUG

spring:
  jmx:
    enabled: true

kafka:
  clusters:
    - name: local
      bootstrapServers: localhost:9092
      metrics:
        port: 11001
        type: PROMETHEUS

auth:
  type: DISABLED

dynamic.config.enabled: true

